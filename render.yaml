services:
  - type: web
    name: bluewave-markets
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ALPHA_VANTAGE_API_KEY
        sync: false
